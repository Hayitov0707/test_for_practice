name: Quadratic CI

on:
  push:
    branches: [ "Quadratic" ]
  pull_request:
    branches: [ "Quadratic" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y build-essential clang-format

      - name: Check Formatting
        run: |
          cd Quadratic_equation
          make check_fmt

      - name: Build and Run Tests
        run: |
          cd Quadratic_equation
          make all
          make test
